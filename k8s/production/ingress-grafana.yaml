# Grafana Ingress for Observability Stack
---
# Grafana Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: identity-system
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/preserve-host: "true"
    konghq.com/plugins: grafana-cors
spec:
  ingressClassName: kong
  rules:
    - host: grafana.fcg-identity.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana-service
                port:
                  number: 3000

---
# CORS para Grafana
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: grafana-cors
  namespace: identity-system
plugin: cors
config:
  origins:
    - "*"
  methods:
    - GET
    - POST
    - PUT
    - DELETE
    - OPTIONS
    - PATCH
  headers:
    - Accept
    - Accept-Version
    - Content-Length
    - Content-MD5
    - Content-Type
    - Date
    - Authorization
    - X-Auth-Token
    - X-Correlation-ID
    - X-Grafana-Org-Id
  exposed_headers:
    - X-Auth-Token
    - X-Correlation-ID
  credentials: true
  max_age: 3600
  preflight_continue: false
